import { IGenComponentComponentsContactForm } from "@/src/types/IGenTypes";

export const ContactForm: React.FunctionComponent<
  IGenComponentComponentsContactForm
> = ({
  id,
  __typename,
  description,
  email,
  formTag,
  formTitle,
  inputs,
  phone,
  title,
}) => {
  return <div>ContactForm</div>;
};
